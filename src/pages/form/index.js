import { Helmet } from "react-helmet";

import Component from "../../components/app/form";

const Form = () => (
  <div>
    <Helmet title="Form" />
    <Component />
  </div>
);

export default Form;
